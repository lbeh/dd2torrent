name: Create torrent
on:
  push:
    branches:
      - master
      
env:
  name: "Android Studio and SDK Tools"
  comment: "Android Studio for Windows"
  url: "Create a repository on GitHub using this template.
  file_name: "https://cdn-151.anonfiles.com/D1DdE1n8z7/2d013ff9-1682723101/OS+E123_720p.mp4"
  piece_size: 20 # Check the README.md for recommended piece size
      
jobs:
  create:
    runs-on: ubuntu-20.04
    steps:
      - name: Install mktorrent
        run: sudo apt-get install mktorrent
        
      - name: Download file
        run: wget -nv -O "$file_name" "$url"
        
      - name: Create torrent using mktorrent
        run: mktorrent -c "$comment" -l $piece_size -o "${name}.torrent" -w "$url" "$file_name"
        
      - name: Upload torrent file
        uses: actions/upload-artifact@v2
        with:
          name: ${{ env.name }}
          path: ${{ env.name }}.torrent
